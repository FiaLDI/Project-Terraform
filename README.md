# New-Game

Это Unity-проект. Этот репозиторий предназначен для командной разработки.  
В нем указаны правила работы с Git и алгоритм развёртывания для новых разработчиков.

---
## 📂 Структура проекта
```
NewGame/
├── Assets/              # Игровые сцены, префабы, скрипты
├── Packages/            # Unity Package Manager
├── ProjectSettings/     # Настройки проекта
├── .gitignore           # Файлы и папки, игнорируемые Git
└── README.md
```

---

## ⚡Правила работы с репозиторием
### 1. Ветки
- `main` — только стабильная версия проекта.
- `develop` — активная разработка.
- `feature/*` — новые фичи.
- `bugfix/*` — исправление багов.

### 2. Работа с ветками
- Каждый разработчик создаёт **свою ветку** от `develop`:
  ```bash
  git checkout develop
  git pull
  git checkout -b feature/имя_фичи
  ```

- Все изменения коммитятся в своей ветке и отправляются на GitHub:

  ```bash
  git add .
  git commit -m "Описание изменений"
  git push origin feature/имя_фичи
  ```

### 3. Pull Request

* Merge в `develop` или `main` только через **Pull Request**.
* Включены проверки:

  * Требуется **одобрение PR** (1–2 approvals для маленькой команды, 2–3 для большой).
  * Merge заблокирован, если есть **Request Changes**.
  * Прямые пуши в `main` запрещены (Branch Protection Rules).

### 4. Коммиты

* Не коммитьте папки:

  ```
  Library/
  Temp/
  Logs/
  MemoryCaptures/
  Builds/
  ```
* Все коммиты должны быть понятными, с описанием изменений.

---

## 🚀 Алгоритм развёртывания для новых разработчиков

1. **Клонируем репозиторий**

```bash
git clone https://github.com/FiaLDI/New-Game.git
cd New-Game
```

2. **Открываем проект в Unity Hub**

* Unity подтянет версию Editor из `ProjectSettings/ProjectVersion.txt`.
* Дождитесь пересборки `Library/`.

3. **Создаём рабочую ветку**

```bash
git checkout -b feature/имя_фичи develop
```

4. **Работаем над проектом**

* Добавляем сцены, префабы, скрипты.
* Игнорируем Library/Temp/Builds.

5. **Сохраняем изменения и пушим**

```bash
git add .
git commit -m "Описание изменений"
git push origin feature/имя_фичи
```

6. **Создаём Pull Request**

* Merge в `develop` (или `main` при готовой версии) только через PR с одобрением коллег.

7. **Синхронизация**

* Перед началом работы:

```bash
git checkout develop
git pull origin develop
```

---

## 🔗 Полезные ссылки

* [GitHub Flow](https://guides.github.com/introduction/flow/) — рекомендуемый рабочий процесс.
* [Unity Version Management](https://docs.unity3d.com/Manual/GettingStartedInstallingHub.html) — управление версиями Unity через Hub.



---
