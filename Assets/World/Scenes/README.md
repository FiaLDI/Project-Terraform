# World Scenes

This folder contains world-related scene entry points.

## Current scenes

- `BootstrapScene` is used for startup and initial runtime bring-up.
- `NetHubScene` is used for network-oriented scene flow and world/session entry.

## Role

These scenes are the scene-level doorway into the rest of the world runtime, rather than a place for reusable gameplay code.
